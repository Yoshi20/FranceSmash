- set_meta_tags description: 'France Smash Calendar'

.row
  .col
    %h1.page__title= t('calendar.title')

-# hidden region images to get the asset_urls via js
.hidden
  - @regions.each do |region|
    = image_tag "regions/#{region}.svg", height: '1px', width: '1px', id: region

= render 'calendar'
